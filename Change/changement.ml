let rec find l elm = match l with
|  


let border tri_set =
    let one_time_seen_triangle = ref [] in
    let supp_triangle
    let length = List.length tri_set in
    for tri_index = 0 to (length - 1) do
        if


let add_point tri_set new_point =
